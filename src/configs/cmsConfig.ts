const cmsConfig = {
  seo: {
    enabled: false, // Set to true to enable SEO features
    robotsOptions: ['all', 'noindex', 'nofollow', 'none', 'noarchive', 'noimageindex', 'notranslate', 'nosnippet']
  },
  menus: {
    targetOptions: ['_self', '_blank']
  }
};

export default cmsConfig;
